{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prom" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 0 },
      "id": 1,
      "options": {
        "legend": { "placement": "bottom", "showLegend": true }
      },
      "targets": [
        {
          "expr": "sum by (type) (rate(scenario_runs_total[1m]))",
          "legendFormat": "{{type}}",
          "refId": "A"
        }
      ],
      "title": "Scenario runs per second (by type)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prom" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 9 },
      "id": 2,
      "options": {
        "legend": { "placement": "bottom", "showLegend": true }
      },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le, type) (rate(scenario_run_duration_seconds_bucket[5m])))",
          "legendFormat": "{{type}}",
          "refId": "A"
        }
      ],
      "title": "Latency p95 (seconds)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prom" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 9 },
      "id": 3,
      "options": {
        "legend": { "placement": "bottom", "showLegend": true }
      },
      "targets": [
        {
          "expr": "sum by (status) (rate(scenario_runs_total{status=~\"error|validation_error\"}[1m]))",
          "legendFormat": "{{status}}",
          "refId": "A"
        }
      ],
      "title": "Error-rate style signals (error + validation_error)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "loki", "uid": "loki" },
      "gridPos": { "h": 11, "w": 24, "x": 0, "y": 18 },
      "id": 4,
      "options": {
        "showLabels": true,
        "showTime": true,
        "wrapLogMessage": true
      },
      "targets": [
        {
          "expr": "{compose_service=\"backend\"}",
          "queryType": "range",
          "refId": "A"
        }
      ],
      "title": "Logs from backend (filter scenarioType in Explore)",
      "type": "logs"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["signal-lab"],
  "templating": { "list": [] },
  "time": { "from": "now-30m", "to": "now" },
  "timezone": "browser",
  "title": "Signal Lab",
  "uid": "signal-lab-dash",
  "version": 1
}
